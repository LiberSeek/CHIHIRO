services:
  chihiro:
    build:
      context: ..
      dockerfile: Dockerfile
    environment:
      NODE_ENV: development
    volumes:
      - ../apps:/app/apps:ro
      - ../backend:/app/backend:ro
      - ../branding:/app/branding:ro
    command: ["node", "--watch", "backend/src/gateway/server.mjs"]
